first(attributes('table))$
'hold_all;

table(0,[3]);
[0,0,0]$

table(i,[i,3]);
[1,2,3]$

table( x^y, [x,1,2], [y,1,2]);
[[1,1],[2,4]]$

table(sin(x),[x,0,2*%pi,%pi/4]);
[0,1/sqrt(2),1,1/sqrt(2),0,-1/sqrt(2),-1,-1/sqrt(2),0]$

/* I broke this
(errcatch( table(i) ), (is (error_code = args_too_few)));
true$
*/
