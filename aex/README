The aex distribution requires that mext and defmfun1 and maxdoc
be built and installed.

Build like this:

(%i1) load("./build.mac");
(%i2) aetest();
(%i3) mext_dist_user_install();

In a new maxima session, aex can be loaded with
(%i1) load(aex);

